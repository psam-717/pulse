package com.example.demo.model;

/**
 * Display-only payment network labels from pulse-mobile payments-store.
 * These are NOT chargeable instruments — Aza hosted checkout owns the rails.
 */
public enum PaymentNetwork {
    mtn_momo,
    telecel_cash,
    card
}
